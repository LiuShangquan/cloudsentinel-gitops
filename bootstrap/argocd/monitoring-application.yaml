apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cloudsentinel-monitoring-lab
  namespace: argocd
  labels:
    app.kubernetes.io/part-of: cloudsentinel
    environment.cloudsentinel.io/name: lab
  annotations:
    argocd.argoproj.io/compare-options: ServerSideDiff=true
spec:
  project: cloudsentinel-platform
  source:
    repoURL: git@github.com:LiuShangquan/cloudsentinel-gitops.git
    targetRevision: main
    path: platform/cloudsentinel-monitoring/overlays/lab
  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring
  syncPolicy:
    automated:
      enabled: true
      prune: false
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
      - ServerSideApply=true
    retry:
      limit: 5
      backoff:
        duration: 10s
        factor: 2
        maxDuration: 3m

